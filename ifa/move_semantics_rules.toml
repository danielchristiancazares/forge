version = 1
last_updated = "2026-02-19"

[[state_bearing_types]]
type_path = "engine::state::ActiveStream"

[[state_bearing_types.consumed_transition_methods]]
method_path = "engine::state::ActiveStream::transition_to_journaled"
consumes_self = true
post_move_unusability_guarantee = "Transient state value is moved and cannot be reused after transition to Journaled."

[[state_bearing_types]]
type_path = "engine::ui::view_state::FocusState"

[[state_bearing_types.consumed_transition_methods]]
method_path = "engine::app::mod::App::set_focus_state"
consumes_self = true
post_move_unusability_guarantee = "Prior focus-state variant is replaced atomically; stale variant data is no longer reachable."

[[state_bearing_types]]
type_path = "engine::state::ToolLoopStart"

[[state_bearing_types.consumed_transition_methods]]
method_path = "engine::app::tool_loop::App::run_tool_loop"
consumes_self = true
post_move_unusability_guarantee = "ToolLoopStart payload ownership is consumed to prevent forked execution paths from reusing the same capability-bearing payload."
